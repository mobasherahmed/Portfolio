/**
 * Single place the resume filename is defined. Replace the PDF in
 * src/assets/cv/ keeping this name and every download link stays correct.
 */
export const CV_FILE_NAME = 'Ahmed-Mobasher-CV.pdf';
export const CV_PATH = `assets/cv/${CV_FILE_NAME}`;
